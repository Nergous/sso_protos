version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Field `isenabled` is not snake_case — tracked as L4 (breaking change).
    - FIELD_LOWER_SNAKE_CASE
breaking:
  use:
    - FILE
